console.clear()
// require("./models/db")
// const express = require("express")
// const cors = require("cors")

// const app = express()

// app.use(cors())
// app.use(express.json())


// app.use('/members',require('./routes/members-login'))

// app.listen(5000,console.log("Backend server under port:5000"))


let member={
    name:"varun",
    number:20,
    bio:{
        age:18,
        address:"1/130 karapakkam"
    },
    test:[
        body={
            data:21,
            height:"122cm",
            weight:"78.kg",
            arm:14,
            chest:41,
            waist:39,
        },
        assessment={
            fiexibility:-1,
            push:""
        }

    ]
}

console.log(member)